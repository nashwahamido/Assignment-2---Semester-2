<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Group Profile — Atlasphere</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/groups.css">
</head>
<body>

<%- include('../partials/navbar', { user: typeof user !== 'undefined' ? user : null }) %>

<div class="profile-page">
  <h1><%= (typeof group !== 'undefined' && group) ? group.name : 'Group' %></h1>
  <p style="color: var(--text-secondary);">Group profile page</p>
  <a href="/groups" class="btn btn-primary" style="margin-top: 24px;">Back to Groups</a>
</div>

<%- include('../partials/footer') %>

</body>
</html>